declare const styles: {
  readonly container: string;
  readonly suggestionsGrid: string;
  readonly suggestionButton: string;
  readonly suggestionText: string;
  readonly loadingContainer: string;
};

export default styles;
