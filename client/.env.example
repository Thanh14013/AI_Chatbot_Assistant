# API Configuration
# Base URL for API requests (optional, defaults to /api which uses proxy)
VITE_API_BASE_URL=/api

# Backend server URL (optional, defaults to http://localhost:3000)
VITE_BACKEND_URL=http://localhost:3000
